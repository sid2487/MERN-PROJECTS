import type { OutputKind } from '../../types.mts';
import type { RangeStyle } from '../../utils/semver.mts';
export declare function handleFix({ autoMerge, cwd, limit, outputKind, purls, rangeStyle, test, testScript }: {
    autoMerge: boolean;
    cwd: string;
    limit: number;
    outputKind: OutputKind;
    purls: string[];
    rangeStyle: RangeStyle;
    test: boolean;
    testScript: string;
}): Promise<void>;
//# sourceMappingURL=handle-fix.d.mts.map