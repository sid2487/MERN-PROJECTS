import type { CResult } from '../../types.mts';
import type { EnvDetails } from '../../utils/package-environment.mts';
import type { RangeStyle } from '../../utils/semver.mts';
export declare function pnpmFix(pkgEnvDetails: EnvDetails, { autoMerge, cwd, limit, purls, rangeStyle, test, testScript }: {
    autoMerge: boolean;
    cwd: string;
    limit: number;
    purls: string[];
    rangeStyle: RangeStyle;
    test: boolean;
    testScript: string;
}): Promise<CResult<{
    fixed: boolean;
}>>;
//# sourceMappingURL=pnpm-fix.d.mts.map